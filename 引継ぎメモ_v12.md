# CRL 引継ぎメモ v12(2026-08-16時点)

v11からの差分のみ記載。**v11メモは引き続き有効**なので併せて参照すること。
本バージョンからClaude Codeへ移行するため、常設ルールは `CLAUDE.md` に分離した。

---

## v12で実装した機能

### 1. ★対面レート格差の警告(最大の新規機能)

同ロール対面のμ差がしきい値以上の組み合わせを検出し、3段階で警告する。**編成はブロックしない**(合意があれば続行)。

- **モーダル**: 編成確定直後に該当レーンを列挙
- **バナー**: マッチングタブ最上部に全幅表示
- **ハイライト**: 結果カードのスロット(左ボーダー+⚠バッジ)、対面比較タブの行(`boxShadow` inset)

#### 設計上の決定事項(重要)

| 論点 | 決定 | 理由 |
|---|---|---|
| μの採用元 | **ライブ値優先**(`players` から引く。無ければ `slot.mu` にフォールバック) | 対面比較タブの既存表示(`statFor`)がライブ値のため、揃えないと同一画面で数値が食い違う |
| 手動入替時のモーダル | **格差レーンが新規に増えた時のみ**(`prevRoles` と比較) | 毎回出すと入替のたびに邪魔になる |
| 自動編成時のモーダル | 毎回 | 結果を初めて見るタイミングなので |
| 判定のソース | 両タブとも `warnRoles`(単一ソース) | タブ間で判定がズレる余地を残さない |
| Firebase保存 | **しない** | `balanceResult` + `settings` から常に再計算できる |
| Discordコピーへの追記 | **なし** | 画像コピーはDOMキャプチャなのでハイライトが自動反映される |

#### しきい値(可変)

- `customstats/settings.matchupWarnThreshold`。既定15pt、範囲5〜60、小数第1位まで
- **管理者PASS付きで変更可**。マッチングタブ「事前固定」直下の1行UI(`editMatchupThreshold`)
- 変更しても**再編成はしない**(表示判定にのみ使うため即座に反映される)
- `session` ではなく `settings` ノードに置く。session クリアと購読時の正規化で未知フィールドが落ちるため

#### 主要シンボル

`MATCHUP_WARN_DEFAULT/MIN/MAX`、`clampMatchupWarn()`、`matchupGaps()`(モジュールスコープの純関数)、
`matchupWarnings` / `warnRoles`(useMemo)、`notifyMatchupGaps()`、`editMatchupThreshold()`

i18n: `balance.074`〜`083`、`scoutMulti.021`〜`022`

---

### 2. League Classic チャンピオン重複の修正

**パッチ26.15(2026/07/29)でLeague Classicが実装され、DDragonの `champion.json` が233件に増加。**
Classic版は `id` が `Jade_` 接頭辞、`key` が `60000 + 元key`、**表示名は通常版と完全に同一**(60件重複)。

そのまま取り込むと、記録タブの入力候補が二重化し、`champImgMap` が後勝ちでアイコンを上書きしてしまう。

```js
const isClassicChamp = (ch) =>
  String(ch.id || "").startsWith("Jade_") || Number(ch.key) >= 60000;
```

除外に加え、表示名でMapに畳み込み **`key` が小さい方(オリジナル)を残す**。将来別系統の重複が増えても耐える。
233件 → 173件。**既存の記録データは日本語表示名を正準値として保存しているため無傷**(表示層のみの問題だった)。

- **アイテム側は問題なし**(実データ確認済み)。Classicアイテムは既存の `maps["11"]` 判定で自然に弾けている模様(未検証の推測)
- **判断**: Classicチャンピオンは別枠にせず統合(除外)。League Classicは別モードだが、勝率統計を分けるほどの運用需要が現時点でないため

---

### 3. 記録タブに参加回数ランキングを追加

- 列: 名前 / 参加回数 / 参加率 / 勝率。上位10名
- **閾値なし**(他ランキングの「5戦以上」等の足切りを設けない。参加を促す指標のため1戦から載る)
- 見出し行の右端に「試合数 ◯◯回」を見出しと同サイズ(18px/700)で表示。汎用の `headRight` プロパティとして実装したので他ランキングにも流用可
- **分母は承認済み全試合数で固定**。ロールフィルタをかけても分母は変わらない(=「全試合のうちTOPで出た割合」の意味になる)

i18n: `records.026`〜`028`

---

### 4. 対面レート差合計の解説を追加

数値表示の横に `?` ボタン。テーマモーダルで解説を表示。
常時表示だと数値行が煩雑になるため、必要時のみ参照する形にした(モバイルでhover不可のためtooltipは不採用)。

内容: 定義 / 具体例(TOP 90対50・JG 50対90でチーム合計差0でも合計80) / 目安(25以下・25〜50・50以上) / アルゴリズム内の重み / 格差警告との使い分け。

i18n: `balance.084`〜`090`

---

### 5. テーマモーダルに `nowrap` オプション追加

`themedAlert(msg, names, { nowrap: true })` で折り返しを無効化(`whiteSpace: "pre"` + `overflowX: auto`)。
モーダル幅を 480px → **720px** に拡大。

**注意**: 当初これを全ダイアログ共通で `pre` にしたところ、既存の長文警告(シーズンリセット確認・重複試合確認・申請承認確認)が折り返しを失い横スクロール必須になる退行が発生した。
**共有コンポーネントを変更する時は、その全呼び出し箇所への影響を確認すること。**

---

## v12で判明した教訓(v11の表に追加)

| バグ/事象 | 教訓 |
|---|---|
| 共通モーダルの `whiteSpace` 変更で既存24箇所が退行 | 共有コンポーネントの変更は全呼び出し箇所を確認。デフォルトを変えずオプトインにする |
| グリッド子要素に `min-width:0` がなく長文がはみ出す | CSS Gridのアイテムは既定で内容の最小幅を保つ。折り返し前提なら `minWidth: 0` が要る |
| League Classicでチャンピオンが二重化 | 外部APIのエントリ増加を前提に、**表示名の一意性を常に担保**する |

---

## 環境上の制約(Claude Code移行の主因)

- **チャット環境のコンテナはDDragonへアクセスできない**(allowlist外)。今回のClassic調査はユーザーにブラウザコンソールでの実行を依頼して判明した。DDragon依存機能が多いため、この往復コストが移行の決め手になった
- zipのアップロード/ダウンロード往復、957KB→1.0MBのビルド成果物の受け渡しも同様に不要になる

---

## 未対応・次にやるべきこと(v11から更新)

1. **承認権限の棚卸し** — 破壊的操作でPASSガードが漏れていないか監査
2. **pending滞留警告** — 前回セッション以前のpendingが残っている場合にバナー表示(画像残留の唯一の残留リスク)
3. バックアップ定期リマインド
4. モバイル実機での横スクロール検証
5. 初見向けの導線
6. 韓国語訳のネイティブチェック
7. Firebase Authentication(荒らし発生時)
8. バッジ・称号システム(構想12種、ネガティブバッジ禁止)
9. **参加率の分母をロールフィルタ連動にするか**(現在は全試合固定)— 実運用を見て判断
10. **Classicチャンピオンを別集計にするか** — League Classicの利用実態を見て判断
